$('.party__slider').slick({
    slidesToShow: 1,
    variableWidth: true,
    infinite: false,
});

$('.party__slider-1').slick({
    slidesToShow: 1,
    variableWidth: true,
    infinite: false,
});

$('.photos__slider').slick({
    slidesToShow: 1,
    centerMode: true,
    arrows: true,
    variableWidth: true,
});
